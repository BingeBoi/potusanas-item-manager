# Free Neon database: https://console.neon.tech
# Create a project, copy the connection string, then paste it here and in your host's env vars.
DATABASE_URL=

# Any long random string. Required when the app is deployed.
SESSION_SECRET=
